/**
 * @file Prettier Configuration
 * @description Re-exports the shared Prettier configuration from the
 *   `@nesvel/prettier-config` package. This keeps formatting rules
 *   consistent across all projects in the Nesvel ecosystem.
 * @see https://prettier.io/docs/en/configuration#sharing-configurations
 */

/** Shared Prettier config package identifier. */
const config: string = '@nesvel/prettier-config';

export default config;
